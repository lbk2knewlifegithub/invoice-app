import { addDays } from "./utils";

describe("utils", () => {
  it("should add 2 days to current date", () => {
    // const current = new Date("20-8-2020").toDateString();
    // expect(addDays(current, 2)).toBe("22-8-21");
  });
});
